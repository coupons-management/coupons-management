$(document).ready(function () {

    /*$('#type_all,#type_code,#type_deal').click(function () {
        $(this).siblings().removeClass('btn-active');
        $(this).addClass('btn-active');
        var id = $(this).attr('id');
        var type;
        switch (id) {
            case 'type_all':
                type = "ALL";
                break;
            case 'type_code':
                type = "CODE";
                break;
            case 'type_deal':
                type = "DEAL";
                break;
        }
        $('.box.d-flex.my-2.my-sm-3').each(function (k, item) {
            if (type === 'ALL') {
                $(this).css("cssText", "display: flex!important");
            } else {
                if ($(this).data('coupon-type') === type) {
                    $(this).css("cssText", "display: flex!important");
                } else {
                    $(this).css("cssText", "display: none!important");
                }

            }
        })
    });*/


})